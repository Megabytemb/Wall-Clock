language: node_js
node_js:
  - "4.2"
sudo: false

script:
  - ./node_modules/.bin/gulp eslint
  - ./node_modules/.bin/gulp unit

cache:
  directories:
    - node_modules

addons:
  sauce_connect: true

after_success:
	- "[[ $TRAVIS_BRANCH == \"master\" ]] && { gulp deploy; };"